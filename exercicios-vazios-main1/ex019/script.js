/* Pergunte ao usuário a base e a altura de um triângulo. Exiba a área desse
triângulo */

let base = parseInt(prompt("Digite a base do triângulo: "))
let altura = parseInt(prompt("Digite a altura do triângulo: "))

let area = 0.5 * base * altura

alert(`Área = ${area}cm`)